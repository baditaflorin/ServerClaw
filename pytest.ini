[pytest]
testpaths =
  tests
norecursedirs =
  .ansible
  roles
markers =
  integration: end-to-end platform integration tests
  mutation: live tests that intentionally mutate platform state
  destructive: opt-in tests with disruptive recovery or failover actions
