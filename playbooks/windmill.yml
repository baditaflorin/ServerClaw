---
- name: Converge the Tailscale operator proxy for Windmill
  hosts: proxmox_hosts
  become: true
  gather_facts: true
  tags:
    - tier-1
    - automation
    - service-windmill

  roles:
    - role: proxmox_tailscale_proxy
    - role: proxmox_security

- name: Converge PostgreSQL access for Windmill
  hosts: "{{ 'postgres-staging-lv3' if (env | default('production')) == 'staging' else 'postgres-lv3' }}"
  become: true
  gather_facts: true
  tags:
    - tier-2
    - automation
    - service-windmill

  pre_tasks:
    - name: Run shared preflight checks
      ansible.builtin.import_tasks: tasks/preflight.yml
      vars:
        playbook_execution_require_debian: true
        playbook_execution_emit_audit: false
        required_hosts:
          - "{{ playbook_execution_required_hosts.postgres[playbook_execution_env] }}"

  roles:
    - role: linux_guest_firewall
    - role: postgres_vm
    - role: windmill_postgres

  post_tasks:
    - name: Restart PostgreSQL before Windmill runtime verification
      ansible.builtin.service:
        name: "{{ postgres_vm_postgresql_service_name }}"
        state: restarted

- name: Converge Windmill on the Docker runtime VM
  hosts: "{{ 'docker-runtime-staging-lv3' if (env | default('production')) == 'staging' else 'docker-runtime-lv3' }}"
  become: true
  gather_facts: true
  tags:
    - tier-2
    - automation
    - service-windmill

  pre_tasks:
    - name: Run shared preflight checks
      ansible.builtin.import_tasks: tasks/preflight.yml
      vars:
        playbook_execution_require_debian: true
        playbook_execution_audit_action: playbook.start.windmill
        playbook_execution_audit_target: windmill
        required_hosts:
          - "{{ playbook_execution_required_hosts.docker_runtime[playbook_execution_env] }}"

  roles:
    - role: linux_guest_firewall
    - role: docker_runtime
    - role: windmill_runtime

  post_tasks:
    - name: Run shared post-verify checks
      ansible.builtin.import_tasks: tasks/post-verify.yml
      vars:
        service_health_probe_id: windmill
        playbook_execution_post_verify_audit_action: playbook.verify.windmill
    - name: Run shared completion notifications
      ansible.builtin.import_tasks: tasks/notify.yml
      vars:
        playbook_execution_notification_service: windmill
