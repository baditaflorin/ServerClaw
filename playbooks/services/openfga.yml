---
# Playbook: services/openfga.yml
# Purpose: Provide a leaf service import for OpenFGA so repo automation can syntax-check and target the service cleanly.
# Use case: Used by generated or scoped automation paths that operate on one service leaf playbook at a time.
#
# Inputs (required):
#   - None.
#
# Inputs (optional):
#   - Same optional inputs as playbooks/openfga.yml.
#
# Outputs:
#   - Imports the top-level OpenFGA converge playbook.
#
# Idempotency: Fully idempotent
#              This file is a pure import wrapper around the top-level playbook.
#
# Dependencies:
#   - ADR 0262: OpenFGA and Keycloak for delegated ServerClaw capability authorization.
#   - Role: openfga_runtime — implemented in the imported playbook.
#
# Related ADRs: 0262
# Related Runbooks: docs/runbooks/configure-openfga.md
#
# Author: LV3 Platform
# Last Updated: 2026-03-29

- import_playbook: ../openfga.yml
