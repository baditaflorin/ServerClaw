---
# Playbook: services/one-api.yml
# Purpose: Provide a leaf service import for One-API so repo automation can syntax-check and target the service cleanly.
# Use case: Used by generated or scoped automation paths that operate on one service leaf playbook at a time.
#
# Inputs (required):
#   - None.
#
# Inputs (optional):
#   - Same optional inputs as playbooks/one-api.yml.
#
# Outputs:
#   - Imports the top-level One-API converge playbook.
#
# Idempotency: Fully idempotent
#              This file is a pure import wrapper around the top-level playbook.
#
# Dependencies:
#   - ADR 0294: One-API as the unified LLM API proxy and router.
#   - Role: one_api_runtime — implemented in the imported playbook.
#
# Related ADRs: 0294
# Related Runbooks: docs/runbooks/configure-one-api.md
#
# Author: LV3 Platform
# Last Updated: 2026-03-30

- import_playbook: ../one-api.yml
