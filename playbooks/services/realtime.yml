---
# Playbook: services/realtime.yml
# Purpose: Expose the ADR 0196 realtime service through the standard service leaf path used by live-apply-service.
# Use case: Run `make live-apply-service service=realtime` for the governed Netdata realtime rollout.
#
# Inputs (required):
#   - proxmox_guest_ssh_connection_mode (str, required): Guest SSH routing mode for the scoped Ansible runner.
#
# Inputs (optional):
#   - bypass_promotion (bool, optional, default=false): Explicit break-glass flag for latest-main live apply flows.
#
# Outputs:
#   - The root realtime playbook is imported through the standard service-specific entrypoint.
#
# Idempotency: Fully idempotent
#
# Dependencies:
#   - ADR 0196: Netdata real-time streaming metrics.
#   - Requires: playbooks/realtime.yml.
#   - Role: lv3.platform.netdata_runtime — invoked through the imported playbook chain.
#
# Related ADRs: 0196
# Related Runbooks: docs/runbooks/configure-netdata.md
#
# Author: LV3 Platform
# Last Updated: 2026-03-27

- import_playbook: ../realtime.yml
