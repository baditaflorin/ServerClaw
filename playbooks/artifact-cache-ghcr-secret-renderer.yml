# Purpose: Delegate cache-only GHCR proxy configuration delivery to the canonical LV3 platform collection playbook.
# Use case: Install the constrained reader bridge and render the fixed private adapter configuration before enabling that adapter.
# Inputs: Same as ../collections/ansible_collections/lv3/platform/playbooks/artifact-cache-ghcr-secret-renderer.yml.
# Outputs: Canonical playbook import only; no direct infrastructure mutation is defined here.
# Idempotency: Yes; imports the managed one-shot delivery playbook.
# Dependencies: canonical lv3.platform artifact-cache-ghcr-secret-renderer playbook source.
---
- import_playbook: ../collections/ansible_collections/lv3/platform/playbooks/artifact-cache-ghcr-secret-renderer.yml
