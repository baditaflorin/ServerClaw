---
- name: Configure the optional Proxmox staging bridge
  hosts: proxmox_hosts
  become: true
  gather_facts: true

  roles:
    - role: lv3.platform.proxmox_network
      tags:
        - network
        - staging
