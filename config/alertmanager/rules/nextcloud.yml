groups:
  - name: lv3-nextcloud
    rules:
      - alert: NextcloudDown
        expr: probe_success{service="nextcloud",probe_kind="readiness"} == 0
        for: 2m
        labels:
          severity: warning
          service: nextcloud
        annotations:
          summary: Nextcloud is unreachable on the public cloud endpoint.
          description: The Nextcloud readiness probe for cloud.lv3.org has been failing for at least two minutes.
          runbook_url: https://docs.lv3.org/runbooks/configure-nextcloud/
