---
semaphore_database_name: semaphore
semaphore_database_user: semaphore_admin
semaphore_database_support_role: semaphore_user
semaphore_postgres_secret_dir: "/etc/{{ platform_identity.unix_prefix }}/semaphore"
semaphore_postgres_password_file: "{{ semaphore_postgres_secret_dir }}/database-password"
semaphore_local_artifact_dir: "{{ repo_shared_local_root }}/semaphore"
semaphore_database_password_local_file: "{{ semaphore_local_artifact_dir }}/database-password.txt"
