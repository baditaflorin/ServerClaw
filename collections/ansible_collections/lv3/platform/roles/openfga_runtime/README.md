# openfga_runtime

Deploy the private OpenFGA runtime on `docker-runtime`, source its runtime
secrets through OpenBao, and expose the authenticated controller path used by
the ServerClaw delegated authorization bootstrap.
