# linux_access

Applies the shared non-root Linux administration baseline.

Inputs: admin identity fields, bootstrap SSH key path, sudo group, and SSH hardening path.
Outputs: a non-root admin user, authorized key, sudoers drop-in, and SSH hardening configuration.
