# netdata_runtime

Deploys Netdata on the Proxmox host and selected guests, streams child metrics
into the monitoring parent on `monitoring-lv3`, and exposes the consolidated
real-time dashboard for the authenticated `realtime.lv3.org` surface.
