# directus_runtime

Converges the Directus runtime, local bootstrap admin, Authentik-backed browser
sign-in wiring, schema bootstrap, and local verification for the repo-managed
Directus data API on `docker-runtime`.
