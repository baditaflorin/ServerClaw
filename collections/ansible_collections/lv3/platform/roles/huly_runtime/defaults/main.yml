---
huly_service_topology: "{{ platform_service_topology | service_topology_get('huly') }}"
huly_public_base_url: "https://{{ huly_service_topology.public_hostname }}"
# Simplified deployment: Huly frontend only.
# Full Huly requires additional backend services (account, transactor, collaborator,
# elastic, mongo, minio). Add those services to the compose file when ready.
huly_image: hardcoreeng/front:latest
