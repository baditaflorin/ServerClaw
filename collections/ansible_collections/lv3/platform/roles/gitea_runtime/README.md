# gitea_runtime

Deploys private Gitea on `docker-runtime-lv3`, wires Keycloak OIDC, mirrors the bootstrap admin and runner tokens under `.local/gitea/`, and installs the repo-managed pre-receive hook template.
