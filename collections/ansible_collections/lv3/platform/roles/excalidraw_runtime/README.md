# excalidraw_runtime

Deploys the repo-managed Excalidraw app and collaboration room on
`docker-runtime`, patches the published frontend to use the shared
`draw.example.com` collaboration origin, and verifies both listeners locally.
