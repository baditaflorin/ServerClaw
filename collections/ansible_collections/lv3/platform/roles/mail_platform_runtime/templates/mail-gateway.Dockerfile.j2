FROM {{ mail_platform_gateway_base_image }}

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY app.py ./
COPY telemetry.py ./

CMD ["sh", "-c", "uvicorn app:app --host 0.0.0.0 --port 8081 --workers ${UVICORN_WORKERS:-1}"]
