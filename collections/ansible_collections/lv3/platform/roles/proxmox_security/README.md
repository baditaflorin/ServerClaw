# proxmox_security

Applies the Proxmox firewall, notifications, ACME, and TFA baseline.

Inputs: management source ranges, notification settings, ACME settings, and TFA metadata.
Outputs: managed firewall policy, notification endpoints and matchers, ACME config, and TFA provisioning state.
