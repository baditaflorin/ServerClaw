# searxng_runtime

Converges a private SearXNG runtime on `docker-runtime-lv3`, keeps the instance
secret key stable across reruns, and publishes the HTTP surface for operators
and agents through the Proxmox host Tailscale proxy.
