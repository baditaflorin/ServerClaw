# netbox_postgres

Provision the PostgreSQL role, database, and mirrored local password file used by the NetBox runtime.

Inputs: `netbox_database_name`, `netbox_database_user`, `netbox_postgres_secret_dir`, `netbox_postgres_password_file`, `netbox_database_password_local_file`.
