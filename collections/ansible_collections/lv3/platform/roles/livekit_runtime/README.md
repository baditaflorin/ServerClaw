# livekit_runtime

Deploy the repo-managed LiveKit runtime on `docker-runtime-lv3`, inject its
API credentials through OpenBao, and verify room lifecycle locally before the
public edge replay is considered complete.
