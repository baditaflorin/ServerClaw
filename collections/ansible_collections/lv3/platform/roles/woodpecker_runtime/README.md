# woodpecker_runtime

Deploys the repo-managed Woodpecker server and agent on `docker-runtime`, prepares the Gitea OAuth application, mirrors controller-local bootstrap artifacts under `.local/woodpecker/`, and verifies the private and edge-published CI API paths.
