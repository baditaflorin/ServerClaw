# proxmox_metrics

Configures the Proxmox external metric server that writes to InfluxDB.

Inputs: mirrored monitoring writer token paths, metric server id, token hash marker path, and monitoring VM host data.
Outputs: a converged Proxmox metrics target and a local token hash marker.
