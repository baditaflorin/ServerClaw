# redpanda_runtime

Converges the private Redpanda single-broker runtime on `docker-runtime`,
preserves the controller-local admin and platform credentials, and verifies the
Kafka API, HTTP Proxy, and Schema Registry path end to end.
