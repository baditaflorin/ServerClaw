---
argument_specs:
  main:
    short_description: Converge the OpenBao runtime, PKI materials, and seeded automation state.
    options:
      openbao_image:
        type: str
        required: false
        description:
          - Container image reference used for OpenBao.
          - Derived at runtime by derive_service_defaults (ADR 0373); the assert in main.yml provides the real guard.
      openbao_container_name:
        type: str
        required: false
        description:
          - Expected running container name used for verification.
          - Derived at runtime by derive_service_defaults (ADR 0373).
      openbao_site_dir:
        type: path
        required: false
        description:
          - Base directory for the OpenBao runtime stack.
          - Derived at runtime by derive_service_defaults (ADR 0373).
      openbao_config_dir:
        type: path
        required: false
        description:
          - Directory containing rendered OpenBao configuration.
      openbao_data_dir:
        type: path
        required: false
        description:
          - Persistent data directory used by OpenBao.
          - Derived at runtime by derive_service_defaults (ADR 0373).
      openbao_log_dir:
        type: path
        required: false
        description:
          - Directory that stores OpenBao logs.
      openbao_policy_dir:
        type: path
        required: false
        description:
          - Directory that stores rendered OpenBao policies.
      openbao_tls_dir:
        type: path
        required: false
        description:
          - Host directory that stores OpenBao TLS material.
      openbao_compose_file:
        type: path
        required: false
        description:
          - Compose file path for the managed OpenBao stack.
          - Derived at runtime by derive_service_defaults (ADR 0373).
      openbao_config_file:
        type: path
        required: false
        description:
          - Main OpenBao configuration file rendered by the role.
      openbao_tls_enabled:
        type: bool
        required: false
        description:
          - Whether OpenBao should expose its TLS listener.
      openbao_http_port:
        type: int
        required: false
        description:
          - Guest-local HTTP port used when TLS is disabled.
      openbao_tls_port:
        type: int
        required: false
        description:
          - Guest-local HTTPS port exposed by OpenBao.
      openbao_cluster_port:
        type: int
        required: false
        description:
          - Cluster communication port used by OpenBao.
      openbao_api_addr:
        type: str
        required: true
        description:
          - API address advertised by the managed OpenBao runtime.
      openbao_cluster_addr:
        type: str
        required: true
        description:
          - Cluster address advertised by the managed OpenBao runtime.
      openbao_tls_server_cert_host_file:
        type: path
        required: true
        description:
          - Host path to the TLS certificate mounted into OpenBao.
      openbao_tls_server_key_host_file:
        type: path
        required: true
        description:
          - Host path to the TLS private key mounted into OpenBao.
      openbao_tls_client_ca_host_file:
        type: path
        required: true
        description:
          - Host path to the client CA bundle mounted into OpenBao.
      openbao_local_artifact_dir:
        type: path
        required: false
        description:
          - Controller-local directory for OpenBao artifacts and secrets.
      openbao_init_local_file:
        type: path
        required: true
        description:
          - Controller-local file that stores the initialization output.
      openbao_runtime_secret_provisioner_approle_local_file:
        type: path
        required: true
        description:
          - Controller-local root-only AppRole artifact used by runtime-secret helper calls instead of the initialization root token.
      openbao_runtime_secret_provisioner_approle_name:
        type: str
        required: false
        default: runtime-secret-provisioner
        description:
          - Narrow AppRole identity that may write registered service KV payloads and mint service AppRole credentials.
      openbao_runtime_secret_provisioner_policy_name:
        type: str
        required: true
        description:
          - Generated least-privilege policy attached to the runtime-secret provisioner.
      openbao_runtime_secret_protected_approle_names:
        type: list
        elements: str
        required: false
        default:
          - controller-automation
          - runtime-secret-provisioner
        description:
          - AppRole identities that the registered runtime-service catalog must never manage.
      openbao_runtime_secret_namespace_overrides:
        type: dict
        required: false
        default:
          rag_context: platform-context
        description:
          - Explicit compatibility mappings from registry keys to pre-existing service KV and AppRole namespaces.
      openbao_userpass_users:
        type: list
        elements: dict
        required: true
        description:
          - Userpass accounts that must exist after convergence.
      openbao_policies:
        type: list
        elements: dict
        required: true
        description:
          - Policies rendered and loaded into OpenBao.
      openbao_approles:
        type: list
        elements: dict
        required: true
        description:
          - AppRole identities provisioned for automation clients.
      openbao_transit_keys:
        type: list
        elements: str
        required: false
        description:
          - Transit keys that must exist after convergence.
      openbao_database_mount_path:
        type: str
        required: false
        description:
          - Mount path used for the database secrets engine.
      openbao_database_name:
        type: str
        required: false
        description:
          - Database configuration name managed inside OpenBao.
      openbao_database_connection_url:
        type: str
        required: false
        description:
          - Connection URL used to manage dynamic database credentials.
      openbao_database_roles:
        type: list
        elements: dict
        required: false
        description:
          - Dynamic database credential roles provisioned by the role.
