# openbao_runtime

Runs OpenBao on `docker-runtime-lv3` with integrated Raft storage, loopback-only API publication, controller-local bootstrap artifacts, scoped auth identities, a Transit engine, and a PostgreSQL dynamic credentials backend.
