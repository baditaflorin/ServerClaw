---
# NOTE: Conventional variables (site_dir, image, compose_file, env_file,
# openbao_*, container_name, internal_port, local_artifact_dir) are derived
# from platform_service_registry — ADR 0373.
dozzle_runtime_site_dir: /opt/dozzle
dozzle_runtime_compose_file: "{{ dozzle_runtime_site_dir }}/docker-compose.yml"
dozzle_runtime_image: "{{ container_image_catalog.images.dozzle_runtime.ref }}"
dozzle_runtime_hub_container_name: dozzle
dozzle_runtime_agent_container_name: dozzle-agent
dozzle_runtime_hub_port: "{{ platform_service_topology | platform_service_port('dozzle', 'internal') }}"
dozzle_runtime_agent_port: "{{ hostvars[platform_topology_host].platform_port_assignments.dozzle_agent_port }}"
dozzle_runtime_hub_inventory_host: "{{ playbook_execution_host_patterns.docker_runtime[playbook_execution_env] }}"
dozzle_runtime_agent_inventory_hosts:
  - "{{ playbook_execution_host_patterns.docker_runtime[playbook_execution_env] }}"
  - "{{ playbook_execution_host_patterns.docker_build[playbook_execution_env] }}"
  - "{{ groups['monitoring'][0] | default('monitoring-lv3') }}"
dozzle_runtime_is_hub: "{{ inventory_hostname == dozzle_runtime_hub_inventory_host }}"
dozzle_runtime_release_check_mode: manual
dozzle_runtime_disable_analytics: true
