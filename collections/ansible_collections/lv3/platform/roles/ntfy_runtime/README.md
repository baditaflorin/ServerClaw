# ntfy_runtime

Deploys the ADR 0299 ntfy push channel on `docker-runtime`, renders the governed topic registry from `config/ntfy/topics.yaml`, and mirrors repo-managed publisher credentials under `.local/ntfy/`.
