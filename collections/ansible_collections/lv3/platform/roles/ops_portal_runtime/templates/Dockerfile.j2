FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY ops_portal ./ops_portal

EXPOSE 8090

CMD ["uvicorn", "ops_portal.app:app", "--host", "0.0.0.0", "--port", "8090"]
