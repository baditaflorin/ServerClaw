# ops_portal_runtime

Deploy the interactive ops portal runtime on `docker-runtime`.

Inputs: portal source paths, copied repo data, compose paths, session secret, gateway URL, and the local listener port.
