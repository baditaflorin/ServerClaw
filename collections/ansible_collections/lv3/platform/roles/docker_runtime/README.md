# docker_runtime

Installs the Docker Engine baseline for the runtime VM.

Inputs: Docker apt repository settings, package lists, admin users, daemon configuration.
Outputs: a running Docker service with the managed daemon config and operator docker-group access.
