---
argument_specs:
  main:
    short_description: Converge the LV3 unified platform API gateway.
    options:
      api_gateway_internal_port:
        type: int
        required: true
        description:
          - Host port exposed by the gateway on runtime-control.
      api_gateway_sources:
        type: list
        elements: dict
        required: true
        description:
          - Controller-local gateway application files copied into the build context.
      api_gateway_config_sources:
        type: list
        elements: dict
        required: true
        description:
          - Controller-local catalog files copied into the runtime config directory.
      api_gateway_authentik_jwks_url:
        type: str
        required: true
        description:
          - Authentik JWKS endpoint used for bearer-token verification.
      api_gateway_authentik_issuer_url:
        type: str
        required: true
        description:
          - Expected Authentik issuer for gateway JWT verification.
