# control_plane_recovery

Provision scheduled control-plane backup exports on `docker-runtime-lv3`, including an OpenBao Agent plus systemd-credentials delivery path for the snapshot token and Windmill database DSN, and backup delivery to `backup-lv3`.
