# depends_on:
#   - playbooks/services/public-edge.yml
---
# Playbook: services/superset.yml
# Purpose: Provide the canonical service-scoped live-apply entrypoint for Superset.
# Use case: Run through `make live-apply-service service=superset env=production` and related guarded wrappers.
#
# Inputs (required):
#   - env (string, required): Target environment selector passed through the shared execution wrapper.
#
# Inputs (optional):
#   - EXTRA_ARGS (string, optional, default=""): Additional ansible arguments passed by the caller.
#
# Outputs:
#   - Imports the root Superset converge playbook used by the service-scoped wrappers.
#
# Idempotency: Matches playbooks/superset.yml.
#
# Dependencies:
#   - ADR 0079: shared playbook execution model
#   - ADR 0292: Superset SQL-first BI decision
#   - Requires: playbooks/superset.yml
#
# Related ADRs: 0079, 0292
# Related Runbooks: docs/runbooks/configure-superset.md
# Author: LV3 Platform
# Last Updated: 2026-03-31
- import_playbook: ../superset.yml
