# Secret Reference

This page lists secret identifiers and contracts only. Secret values are intentionally excluded.

| Secret ID | Owner Service | Storage Contract | Rotation |
| --- | --- | --- | --- |
{% for row in secrets -%}
| `{{ row.id }}` | {{ row.owner_service }} | `{{ row.storage_contract }}` | {{ row.rotation_summary }} |
{% endfor %}
