# Release 0.117.0

- Date: 2026-03-24

## Summary
- implemented ADR 0117 on current mainline with the new [`platform/graph/`](/Users/live/Documents/GITHUB_PROJECTS/proxmox-host_server-dependency-graph/platform/graph) package, the Postgres migration [`migrations/0012_graph_schema.sql`](/Users/live/Documents/GITHUB_PROJECTS/proxmox-host_server-dependency-graph/migrations/0012_graph_schema.sql), and repo-managed graph manifest [`config/dependency-graph.yaml`](/Users/live/Documents/GITHUB_PROJECTS/proxmox-host_server-dependency-graph/config/dependency-graph.yaml)
- added Windmill-compatible graph rebuild and health-propagation workers under [`config/windmill/scripts/graph/`](/Users/live/Documents/GITHUB_PROJECTS/proxmox-host_server-dependency-graph/config/windmill/scripts/graph), seeded their disabled-by-default Windmill definitions, and exposed traversal plus derived-health endpoints under `/v1/graph/*` in the platform API gateway
- switched ADR 0116 risk-scoring fanout to prefer live `DependencyGraphClient().descendants()` when `LV3_GRAPH_DSN` is configured, added the ADR 0117 operator runbook, and marked ADR 0117 plus its workstream as implemented in repo version `0.117.0`

## Platform Impact
- no live platform version bump; this release updates repository automation, query surfaces, and operator tooling only

## Upgrade Guide
- apply the Postgres migration from `main`, set `LV3_GRAPH_DSN` for the API gateway and worker environments, seed the new Windmill graph scripts, and then enable the graph rebuild schedules after the first live validation run
