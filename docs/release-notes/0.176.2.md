# Release 0.176.2

- Date: 2026-03-27

## Summary
- implement ADR 0181 with [scripts/control_metadata_witness.py](/Users/live/Documents/GITHUB_PROJECTS/proxmox_florin_server/scripts/control_metadata_witness.py), which builds a recoverable `repo.bundle` plus immutable `repo-snapshot.tar`, verifies the selected git remote witness, and promotes new off-host generations only after checksum and clone validation pass
- wire the ADR 0181 witness publication into [playbooks/control-plane-recovery.yml](/Users/live/Documents/GITHUB_PROJECTS/proxmox_florin_server/playbooks/control-plane-recovery.yml) so a live control-plane-recovery apply now republishes the off-host witness only after the restore drill succeeds, while preserving the previous `latest` pointer on failure
- add focused integrity and failure-recovery coverage in [tests/test_control_metadata_witness.py](/Users/live/Documents/GITHUB_PROJECTS/proxmox_florin_server/tests/test_control_metadata_witness.py) and extend the DR/reporting surfaces so [scripts/generate_dr_report.py](/Users/live/Documents/GITHUB_PROJECTS/proxmox_florin_server/scripts/generate_dr_report.py) plus the DR runbooks call out witness health and verification commands

## Platform Impact

- repository version advances to 0.176.2; platform version remains 0.130.22 because the ADR 0181 witness publication path has not yet been live-applied from current `main` with a real off-host archive mount and receipt

## Upgrade Guide

- configure `LV3_CONTROL_METADATA_WITNESS_ARCHIVE_ROOT` on the controller before the next `make converge-control-plane-recovery` run
- optionally set `LV3_CONTROL_METADATA_WITNESS_GIT_REMOTE` and `LV3_CONTROL_METADATA_WITNESS_GIT_REMOTE_REF` when the live apply should verify a non-default off-host git witness
