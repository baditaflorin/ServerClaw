# Release 0.177.78

- Date: 2026-03-29

## Summary
- implemented ADR 0265 by replacing mutable remote validation mirrors with immutable content-addressed repository snapshots, fresh per-run build-server namespaces, and repository-shape-safe remote schema and dependency checks on the newest exact `main`

## Platform Impact
- Platform version advances to 0.130.53 because the newest exact-main replay on 2026-03-29 verified `make check-build-server` and `make remote-validate` on `docker-build`, and the final publish path re-runs the full pre-push gate on the exact commit delivered to `main`.

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
