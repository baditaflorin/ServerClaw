# Release 0.98.0

- Date: 2026-03-23

- implemented ADR 0101 in repository automation with a canonical `config/certificate-catalog.json`, schema validation, and shared TLS expiry probing wired into the observation loop and drift detection
- added `scripts/generate_cert_renewal_config.py` plus reusable `cert_renewal_timer` and `cert_renewer_sidecar` roles so certificate renewal ownership is machine-readable and the first repository-managed OpenBao renewal timer is rendered from code
- extended `config/health-probe-catalog.json` with certificate references, added the certificate-expiry alert rule group under `config/alertmanager/rules/platform.yml`, and documented emergency manual rotation in `docs/runbooks/cert-expired.md`
- updated ADR 0101 plus the workstream registry to mark the repository implementation as complete in `0.98.0`

Platform impact:

- no live platform version bump; this release adds repository-side certificate inventory, renewal automation, probing, and operator runbook coverage only
