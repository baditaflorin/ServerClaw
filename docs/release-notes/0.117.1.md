# Release 0.117.1

- Date: 2026-03-24

## Summary
- implemented ADR 0112 on top of current mainline with the deterministic goal compiler under `platform/goal_compiler/`, rule and alias tables under `config/`, and repo-package loading for the ADR `platform/...` surfaces
- updated `lv3 run` to preserve the existing risk-scored direct workflow path while also compiling natural-language instructions into YAML `ExecutionIntent` documents, writing intent lifecycle events to the mutation ledger, and rejecting unmatched text with explicit `PARSE_ERROR` output
- integrated ADR 0112 with the current mainline ledger and world-state implementations by adding repo-local fallback modes for file-backed intent events and snapshot-backed scope binding, then updated the ADR/workstream metadata to mark the implementation as complete in repo version `0.117.1`

## Platform Impact
- no live platform version bump; repository version advances to `0.117.1` while platform version remains `0.114.2`

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
