# Release 0.176.0

- Date: 2026-03-27

## Summary
- integrated ADR 0117 on `main` with the repaired Windmill graph runtime replay: graph DSNs now flow through the managed Windmill runtime env contract, the worker wrappers fall back through isolated `uv` execution when sandbox dependencies are missing, and the converge path refreshes `world_state.current_view` before graph imports read ADR 0113 state
- recorded the verified 2026-03-26 ADR 0117 production live apply on `docker-runtime-lv3`, `postgres-lv3`, and the platform API gateway, including the workstream-local receipt, runbook updates, and ADR metadata showing platform implementation at `0.130.20`
- merged the latest mainline Windmill runtime contract with ADR 0117 so the repo-unique `windmill-runtime.env{,.ctmpl}.j2` templates now carry both the Proxmox worker token inputs and the graph, world-state, and ledger DSNs, with focused Windmill/API tests and syntax checks passing on the integration branch

## Platform Impact
- live platform version advances to 0.130.23 after the already-verified ADR 0117 dependency-graph runtime is integrated on main, including graph DSN wiring in Windmill, populated world-state refresh handling, and authenticated /v1/graph traversal on api.lv3.org

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
