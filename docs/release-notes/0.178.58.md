# Release 0.178.58

- Date: 2026-04-08

## Summary
- ADR 0364 native build server gate execution: LV3_NATIVE_EXECUTION=1 flag and native_command field in validation-gate.json eliminate per-check Docker overhead on the build server and amd64 emulation on arm64 controller hosts; adds Ansible playbook (build-server-gate-tools.yml) to install and pin gate tool versions on docker-build-lv3

## Platform Impact
- no live platform version bump; this release adds gate execution optimisation and tool provisioning automation only

## Upgrade Guide
- No special upgrade steps required.
