# Release 0.176.8

- Date: 2026-03-27

## Summary
- integrated ADR 0101 automated certificate lifecycle management on `main`, including the final release/state bookkeeping and a mainline live-apply receipt for the verified replay
- hardened the `step_ca_runtime` and `openbao_runtime` converges so repeated live replays survive transient SSH/API churn instead of failing after the service is already healthy
- re-verified the live OpenBao, Vaultwarden, and step-ca certificate paths plus the repo validation gates from the dedicated main-integration worktree

## Platform Impact
- platform_version advances to 0.130.24; ADR 0101 is now live-applied on main with resilient step-ca/OpenBao replays and active OpenBao/Vaultwarden renewal timers.

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
