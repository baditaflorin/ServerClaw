# Release 0.84.0

- Date: 2026-03-23

- completed ADR 0076 in repository automation by enforcing reserved subdomain prefixes, validating repo-managed NGINX edge routes against the canonical subdomain catalog, and documenting the hostname lifecycle in a dedicated runbook
- added `playbooks/provision-subdomain.yml`, `make provision-subdomain FQDN=<hostname>`, and matching workflow and command catalog contracts so already-catalogued hostnames can be converged through the Hetzner DNS role and, when applicable, the shared edge-publication path
- added focused regression coverage for ADR 0076 validation behavior and repaired the current-main `ansible-lint` idempotency failure in `roles/docker_runtime/tasks/main.yml` so the full repository gate passes cleanly again on the integrated branch
- updated ADR 0076 and its workstream metadata to record repository implementation on `0.84.0`

Platform impact:

- no direct live platform change is claimed in this release commit; the governed subdomain workflow is ready for deliberate use from `main`
