# Release 0.175.2

- Date: 2026-03-26

## Summary

- integrated ADR 0106 on `main` with the Windmill worker credential bridge that mirrors the Proxmox API token payload into `/srv/proxmox_florin_server/.local/proxmox-api/lv3-automation-primary.json` and lets the `ephemeral_vm_reaper` wrappers fall back to that repo-local payload when sandboxed jobs do not inherit the runtime env contract
- renamed the Windmill runtime env templates to the repo-unique `windmill-runtime.env{,.ctmpl}.j2` names, refreshed the image-catalog references, and kept repository data-model validation green after the live-apply fixes
- added focused ADR 0106 coverage for the Windmill reaper wrappers, repo-surface expectations, and the mirrored worker credential path in `tests/test_fixture_manager.py`, `tests/test_ephemeral_vm_reaper.py`, `tests/test_fixture_expiry_reaper.py`, and `tests/test_ephemeral_lifecycle_repo_surfaces.py`
- recorded the integrated-mainline ADR 0106 verification path: focused tests and validators passed, the final targeted worker-checkout refresh plus targeted `f/lv3/ephemeral_vm_reaper` sync left the live script at hash `a2eed27d62fa68fc`, `f/lv3/windmill_healthcheck` plus `f/lv3/ephemeral_vm_reaper` both returned HTTP 200, and `docker-runtime-lv3` wrote `.local/fixtures/reaper-runs/reaper-run-20260326T171957Z.json` after the manual sweep

## Platform Impact

- repository version advances to 0.175.2; live platform version advances to 0.130.22 after the rebased ADR 0106 mainline integration refreshed the Windmill worker credential bridge, confirmed the enabled `f/lv3/ephemeral_vm_reaper_every_30m` schedule, and recorded a clean manual reaper receipt under the worker-local `.local/fixtures/reaper-runs/` path on `docker-runtime-lv3`

## Upgrade Guide

- [../upgrade/v1.md](../upgrade/v1.md)
