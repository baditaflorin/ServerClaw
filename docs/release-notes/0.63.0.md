# Release 0.63.0

- Date: 2026-03-22

- applied ADR 0051 live by converging the repository-managed control-plane recovery workflow across `docker-runtime`, `backup`, and the Proxmox host backup-store firewall contract
- hardened the recovery automation so OpenBao is unsealed explicitly for managed Raft snapshots and the runtime archive push path now depends on a repo-managed `backup` guest-firewall allow rule for `10.10.10.20/32`
- recorded the live recovery evidence, restore-drill result, controller bundle mirror, ADR metadata, runbook updates, and canonical stack state needed to keep control-plane backup and break-glass posture current on `main`

Platform impact:

- control-plane recovery archives now land on `backup` under `/srv/control-plane-recovery/runtime/docker-runtime/latest`
- the mirrored controller recovery bundle now lives under `/srv/control-plane-recovery/controller/controller-recovery-bundle.tar.zst`
- the scheduled restore drill on `backup` last passed at `2026-03-22T21:29:48Z`
