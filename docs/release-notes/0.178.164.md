# Release 0.178.164

- Date: 2026-04-24

## Summary
- ADR 0438 Phase 6: `proxmox_vm_templates` keys renamed from `lv3-*` to generic names (`debian-base`, `docker-host`, `postgres-host`, `ops-base`); `proxmox_base_template` role default lookups fixed from broken Jinja2 double-interpolation to static key `'debian-base'`; all `template_key` references updated in `platform.yml`, `host_vars/proxmox-host.yml`, and `publication/templates/proxmox-host.yml` — unblocks `make converge-site` on example.org

## Platform Impact
- no live platform version bump; this release updates repository automation, release metadata, and operator tooling only

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
