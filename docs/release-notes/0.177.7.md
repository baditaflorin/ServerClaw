# Release 0.177.7

- Date: 2026-03-27

## Summary

- completed ADR 0113 from the latest `main` with a durable Windmill replay that keeps the world-state runtime env, worker checkout, and scheduled refresh path aligned on production
- fixed the last live runtime gaps by installing `make` for `opentofu_drift`, installing `python3-psycopg` for host-side `WorldStateClient` checks, and preserving the host-networked Windmill runtime contract introduced by the earlier ADR 0113 repo work
- verified end to end that `world_state.current_view` holds all nine surfaces with `is_expired = false` and that `WorldStateClient().list_stale()` returns `[]` on `docker-runtime-lv3`

## Platform Impact

- repository version advances to 0.177.7
- platform version advances to 0.130.28 after the successful latest-main ADR 0113 replay

## Upgrade Guide

- [../upgrade/v1.md](../upgrade/v1.md)
