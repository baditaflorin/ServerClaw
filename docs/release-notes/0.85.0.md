# Release 0.85.0

- Date: 2026-03-23

- finalized the ADR 0082 remote build gateway for the real live build path by teaching `scripts/remote_exec.sh` to honor config-driven SSH options and by fixing rsync transport handling for ProxyCommand-based SSH hops
- replaced the placeholder build-server host contract with the verified controller -> Proxmox host -> `docker-build-lv3` route, moved the remote workspace root to `/home/ops/builds/proxmox_florin_server`, and documented the jump-path operating model in the gateway runbook
- added regression coverage for configured SSH options, updated the build-guest package baseline to include `rsync`, excluded controller-local validation and assistant worktree caches from rsync sync, and marked ADR 0082 plus its workstream as live applied with a structured receipt

Platform impact:

- `make check-build-server` now passes against the real live build host through `ops@100.118.189.95` to `ops@10.10.10.30`, and the platform version advances to `0.38.0`
