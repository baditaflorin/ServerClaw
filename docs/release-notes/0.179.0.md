# Release 0.179.0

- Date: 2026-04-27

## Summary
- ADR 0443 — Continuous Topology Reconciler & Drift Detection: shipped all phases (stdlib-only `topology_probe.py`, controller `topology_reconciler.py` with SSH fan-out + diff, `validate_no_hardcoded_topology.py` write-time linter wired into `scripts/validate_repo.sh` as a hard gate, hourly Windmill schedule `f/lv3/topology_drift_check_hourly`, ops-portal receipt under `receipts/drift-reports/`).
- ADRs 0439/0440/0441/0442 — Multi-deployment platform foundations (per-deployment identity, artifact-path isolation, profile resolver, deployment-aware tooling).
- Make targets: `validate-topology-templates`, `topology-probe-self`, `detect-topology-drift`, `plan-topology`.
- Per-line `# noqa: topology-hardcode` opt-out for legitimate registry-IP defaults; allowlist tightened to data-source paths.

## Platform Impact
- no live platform version bump; this release updates repository automation, release metadata, and operator tooling only

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
