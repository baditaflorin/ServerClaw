# Release 0.177.70

- Date: 2026-03-29

## Summary
- implemented ADR 0235 by adding a PatternFly-style cross-application launcher to the interactive ops portal, with catalog-backed purpose grouping, persona filtering, favorites, and recent destinations across first-party browser surfaces
- records ADR 0228 exact-main evidence by verifying Windmill as the default browser-first and API-first operations surface, preserving the seeded workflow catalog on the live control plane, and hardening raw-app sync plus bounded seed-script API waits so ignored artifacts or stalled responses no longer wedge the worker replay path

## Platform Impact
- platform version advances to 0.130.48 after the exact-main replays verify both the PatternFly-style cross-application launcher and session-scoped favorites on `ops.lv3.org` and the live CE v1.662.0 Windmill default operations surface, representative workflow execution, and mainline post-merge validation gate on top of the 0.130.47 baseline

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
