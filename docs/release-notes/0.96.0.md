# Release 0.96.0

- Date: 2026-03-23

- completed ADR 0078 in repository automation by adding the collection-native `make scaffold-service` generator, the `make pin-image` helper, and the scaffold runtime-role templates under `collections/ansible_collections/lv3/platform/roles/_template/service_scaffold/`
- expanded the service scaffold to write the current mainline contract surfaces: ADR and workstream docs, runbook, playbooks, service topology, service capability catalog, health probes, subdomains, secret catalogs, controller-local secret manifest, image catalog entry, and placeholder image scan receipt
- updated service-catalog validation so planned services can declare health-probe contracts before rollout, and added a data-model placeholder guard that blocks merges while any scaffold `TODO` marker remains in topology or catalogs
- documented the operator workflow for scaffolding and completing a new service in `docs/runbooks/scaffold-new-service.md` and updated ADR 0078 plus the workstream registry to mark the repository implementation as complete in `0.96.0`
- repaired the inherited `make validate` failure on current `main` by restoring a literal `prevent_destroy = true` lifecycle guard in the shared `tofu/modules/proxmox-vm` module so OpenTofu validation passes again

Platform impact:

- no live platform version bump; this release adds repository-side service onboarding automation and validation only
