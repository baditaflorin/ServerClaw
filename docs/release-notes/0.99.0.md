# Release 0.99.0

- Date: 2026-03-23

- implemented ADR 0099 in repository automation with `scripts/restore_verification.py`, guest-specific smoke-test helpers, and the Windmill `restore-verification` wrapper for scheduled weekly restore drills
- added repository-managed restore verification receipts under `receipts/restore-verifications/`, the `make restore-verification` entry point, and the command plus workflow catalog contracts needed to route the workflow through the governed control plane
- extended the monitoring dashboard templates with backup-restore summary panels and documented the operator flow in `docs/runbooks/backup-restore-verification.md`
- added targeted test coverage for the restore verification runner and Windmill wrapper, and updated ADR 0099 plus the workstream registry to mark the repository implementation as complete in `0.99.0`

Platform impact:

- no live platform version bump; the restore-verification schedule, notification environment, and staging bridge path still need apply-time confirmation from `main`
