# Release 0.178.163

- Date: 2026-04-24

## Summary

- ADR 0438 Phase 5 (partial): `common_openbao_compose_env_openbao_address` now derived from `platform_service_topology.openbao.private_ip` in `group_vars/all/main.yml` — eliminates last hardcoded production IP from `fork-overrides.yml`; fork deployments automatically get the correct OpenBao address from their topology inventory
- `openbao_postgres_backend/defaults/main.yml`: `openbao_postgres_connect_role` changed from hardcoded `lv3_openbao_connect_all` to `{{ platform_sql_prefix }}_openbao_connect_all` — dead-code hardcoding removed

## Platform Impact

- `make bootstrap` on example.org: all 7 concrete ADR 0438 bootstrap failures now addressed; `fork-overrides.yml` retains only legitimate topology decisions (no prefix-flavor, credential-path, or hardcoded-IP workarounds remain)
- Production: byte-identical — `platform_service_topology.openbao.private_ip` resolves to `10.10.10.92` same as before

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
