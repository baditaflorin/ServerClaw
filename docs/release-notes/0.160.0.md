# Release 0.160.0

- Date: 2026-03-26

## Summary
- finish the current-`main` live apply for ADR 0162 by reconciling the Windmill runtime, PostgreSQL password state, and the 30-second `detect_deadlocks` schedule on production
- fix `config/windmill/scripts/detect-deadlocks.py` so the Windmill worker imports `platform.*` from the mounted repo checkout at `/srv/proxmox-host_server` instead of the temporary wrapper path
- record ADR 0153 and ADR 0162 as live on platform version `0.130.11` while preserving ADR 0161 on the already-recorded platform version `0.130.10`

## Platform Impact
- repository version advances to `0.160.0`
- platform version advances to `0.130.11` because the 2026-03-26 mainline live apply verified the Windmill script `f/lv3/detect_deadlocks`, the enabled schedule `f/lv3/detect_deadlocks_every_30s`, and a successful manual `run_wait_result` for the ADR 0162 detector on top of the already-integrated ADR 0161 live state

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
