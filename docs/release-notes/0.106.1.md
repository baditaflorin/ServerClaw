# Release 0.106.1

- Date: 2026-03-24

- corrected the ADR 0093 live rollout contract so `ops_portal` uses `8092` on `docker-runtime-lv3`, avoiding the existing GlitchTip host-network listener already occupying `8090`
- added the missing `playbooks/services/ops_portal.yml` service entrypoint so the standard `make live-apply-service service=ops_portal env=production` path resolves the correct playbook
- made `scripts/mutation_audit.py` add its own `scripts/` directory to `sys.path` so the Ansible mutation-audit callback loads correctly from a clean controller environment
- updated the ops portal health probe, service catalog, API gateway catalog, runbook, and runtime defaults to reflect the new managed host port

Platform impact:

- applied the ADR 0093 live rollout from the corrected `0.106.1` repository snapshot on `2026-03-24`
- the live platform version is now `0.106.1`
