# Release 0.91.0

- Date: 2026-03-23

- completed ADR 0087 in repository automation by adding the fast-hook and remote-first pre-push validation gate, the shared `config/validation-gate.json` manifest, the hook install target, explicit bypass receipts, gate-status reporting, and the Windmill post-merge gate runner
- documented the operator workflow for installing, running, bypassing, and checking the validation gate
- repaired repository validation so one historical ADR 0077 live-apply receipt continues to validate while the workflow catalog remains the canonical source for current execution contracts

Platform impact:

- no live platform version change; this release updates repository automation, release metadata, and operator workflow surfaces only
