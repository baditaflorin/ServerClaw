# Release 0.172.0

- Date: 2026-03-26

## Summary

- fix the shared edge-publication rsync path so worktree-based `configure-edge-publication` runs use the active Ansible private-key context instead of an out-of-scope preflight manifest variable
- complete the first ADR 0134 production live publication on top of the latest `origin/main`, republishing the authenticated changelog and docs portals through `nginx-lv3` with the redacted changelog history artifact deployed
- refresh the generated dependency-graph document so the repository gate matches the current catalog and generated docs state

## Platform Impact

- repository version advances to 0.172.0; platform version advances to 0.130.20 with the ADR 0134 changelog-redaction live-apply receipt recorded as the latest `public_edge_publication` evidence

## Upgrade Guide

- [../upgrade/v1.md](../upgrade/v1.md)
