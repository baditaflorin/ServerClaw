# Release 0.114.1

- Date: 2026-03-24

## Summary
- corrected the ADR 0093 live rollout contract so `ops_portal` uses host port `8092` instead of colliding with the existing GlitchTip host-network listener on `docker-runtime`
- added the missing `playbooks/services/ops_portal.yml` service entrypoint and made `scripts/mutation_audit.py` self-contained so `make live-apply-service service=ops_portal env=production` works from a clean controller environment
- recorded the verified ADR 0093 live rollout at `ops.example.com`, updated the ADR/workstream status to `live_applied`, and captured the live-apply receipt in the canonical platform state

## Platform Impact
- live platform version remains 0.105.0; this release records the verified ADR 0093 live rollout, the repository fixes required to converge it, and the updated ADR/workstream metadata

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
