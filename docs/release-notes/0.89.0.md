# Release 0.89.0

- Date: 2026-03-23

- repaired the ADR 0077 live rollout path by fixing Windmill and Mattermost runtime secret payload construction, teaching every migrated OpenBao Agent sidecar to stay root inside the container, and extending regression coverage for those runtime-secret invariants
- fixed the mail gateway runtime image so it now copies `telemetry.py`, launches `uvicorn` explicitly on `:8081`, and keeps the authenticated mail-platform readiness probe aligned with the gateway API contract
- added focused validation for the compose runtime env guard and health probe contract updates that were exercised during the final live apply from `main`

Platform impact:

- this release carries the repo changes used for the successful ADR 0077 live rollout that is recorded separately in the deployment receipt and platform-state update
