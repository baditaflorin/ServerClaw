# Release 0.90.0

- Date: 2026-03-23

- repaired ADR 0083's remote archive execution path by teaching live-apply receipt validation to fall back to commit-hash format checks when `.git` metadata is intentionally absent from the synced build-server workspace
- added focused regression coverage for archive-mode receipt validation so `validate-schemas` remains green under the Docker-based check-runner flow used on `docker-build`
- re-verified the pinned check-runner images and manifest-backed validation path on `docker-build`, including `lint-yaml`, `type-check`, and `validate-schemas`

Platform impact:

- no live platform version change; this release repairs repository-side validation behavior for the existing build-server automation
