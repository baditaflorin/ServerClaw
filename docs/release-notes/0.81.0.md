# Release 0.81.0

- Date: 2026-03-23

- completed ADR 0081 in repository automation by adding a generated deployment-history portal, a shared receipt or promotion or mutation-audit history parser, and a governed `get-deployment-history` query surface
- wired the new portal into `make generate-status`, repository validation, the public-edge static publication model, the service and subdomain catalogs, and release-note storage under `docs/release-notes/`
- updated ADR 0081 and its workstream metadata to mark the repository implementation complete while leaving live publication pending a deliberate apply from `main`

Platform impact:

- no direct live platform change is claimed in this release commit; `changelog.lv3.org` and the generated portal publication path are now ready to be applied from `main`
