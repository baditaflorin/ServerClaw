# Release 0.164.0

- Date: 2026-03-26

## Summary
- complete the current-main ADR 0167 rollout so the API gateway publishes declared degradation state through `/v1/platform/degradations`, `/v1/platform/services`, and `/v1/platform/health`
- keep Keycloak dependency failures operator-visible after ADR 0166 by preserving the structured circuit-open context inside the canonical error envelope
- ship the authenticated NATS `CONNECT` fix so the gateway clears its durable outbox and degraded state automatically once publication recovers

## Platform Impact
- repository version advances to `0.164.0`; platform version advances to `0.130.15` after the successful ADR 0167 replay from `main`.

## Upgrade Guide
- [../upgrade/v1.md](../upgrade/v1.md)
