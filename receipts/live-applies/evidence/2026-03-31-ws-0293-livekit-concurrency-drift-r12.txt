ws-0293 livekit branch concurrency drift analysis
Date: 2026-03-31
Branch: codex/ws-0293-live-apply-r2
Context: post-r11 failure investigation while holding host:proxmox_florin, vm:110, and vm:120/service:livekit locks

Summary
- The `2026-03-31-ws-0293-livekit-branch-converge-r11.txt` replay succeeded through Proxmox ingress programming, Docker runtime convergence, local LiveKit listener checks, local room lifecycle verification, SBOM collection, and NGINX publication.
- The only replay failure remained the final controller-side public room lifecycle probe against `https://livekit.lv3.org`.
- Direct TLS verification from the controller returned a hostname mismatch because `livekit.lv3.org` was serving the shared `nginx.lv3.org` certificate instead of the site-local `livekit.lv3.org` certificate.

Key observations
- Controller probe:
  - `curl --max-time 20 -svI https://livekit.lv3.org`
  - returned a certificate with `subject: CN=nginx.lv3.org`
  - failed with `subjectAltName does not match host name livekit.lv3.org`
- Edge host certificate on disk:
  - `/etc/letsencrypt/live/livekit.lv3.org/fullchain.pem`
  - `subject=CN=livekit.lv3.org`
  - `DNS:livekit.lv3.org`
- Edge host presented certificate over loopback/SNI:
  - `openssl s_client -servername livekit.lv3.org -connect 127.0.0.1:443`
  - returned the shared `lv3-edge` certificate with `subject=CN=nginx.lv3.org`

Fresh render comparison
- Fetched live file:
  - `/etc/nginx/sites-available/lv3-edge.conf`
  - mtime `2026-03-31 21:37:44 UTC`
  - size `160510`
- Fresh host-side render to `/tmp/ws0293_remote_render.conf` using the current branch template, current inventory, and current hostvars:
  - mtime `2026-03-31 21:41:39 UTC`
  - size `185942`
- The fresh render contains:
  - `livekit.lv3.org` server blocks
  - site-local certificate wiring at `/etc/letsencrypt/live/livekit.lv3.org/fullchain.pem`
  - `flags.lv3.org` server blocks
- The live file on disk does not contain:
  - `livekit.lv3.org`
  - `flags.lv3.org`
- Therefore the branch template and current hostvars are correct; the live file was overwritten after the r11 replay by a concurrent shared-surface mutation.

Timing evidence
- `r11` final failure time:
  - `2026-03-31T21:33:36Z`
- Live edge file overwrite time:
  - `2026-03-31 21:37:44 UTC`
- Fresh diagnostic host-side render time:
  - `2026-03-31 21:41:39 UTC`

Concurrent conflicting applies observed on the controller after the overwrite
- `ws-0279-main-publish-r1` `playbooks/services/outline.yml`
- `ws-0274-mainline-refresh-v6` `playbooks/services/minio.yml`
- `ws-0286-live-apply-r2` `playbooks/vikunja.yml`
- `ws-0292-live-apply` `playbooks/superset.yml`
- `ws-0292-live-apply-r2` `playbooks/services/lago.yml`
- `ws-0303-main-integration` `playbooks/monitoring-stack.yml`
- `ws-0277-main-closeout` `playbooks/services/typesense.yml`

Conclusion
- `r11` no longer indicates a branch-side LiveKit defect.
- The remaining blocker is an unstable shared mutation window on `proxmox_florin`, `docker-runtime-lv3`, and `nginx-lv3`.
- The next valid step is to wait for a quiet shared-host window, then rerun the LiveKit replay and public verification before any merge-to-main step.
