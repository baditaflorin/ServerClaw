# ws-0331 Windmill controller URL diagnosis
# recorded_on: 2026-04-05

Observed issue:
- The exact-main runtime-control live apply stalled at the controller-local task
  "Wait for the Windmill API before syncing repo-managed scripts".
- That task is delegated to localhost but uses windmill_private_base_url
  (10.10.10.92:8000), which is not reachable from the controller.

Controller-side probes:
- curl http://100.64.0.1:8005/api/version => CE v1.662.0
- curl http://10.10.10.92:8000/api/version => curl: (28) Connection timed out after 10008 milliseconds

Relevant task definitions:
- main.yml:1518 remote host wait uses windmill_private_base_url and succeeds from runtime-control-lv3
- main.yml:2183 controller-local wait uses windmill_private_base_url and stalls on localhost reachability
- main.yml:2196/2264/2462 controller-local sync commands also pass windmill_private_base_url
- verify.yml later already uses windmill_base_url for controller-local script execution flows

Action:
- Stop the stalled r19 attempt and switch controller-local Windmill sync calls to windmill_base_url before replaying.
