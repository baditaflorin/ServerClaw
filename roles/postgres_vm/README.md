# postgres_vm

Applies the PostgreSQL guest baseline and database access policy.

Inputs: package list, PostgreSQL service settings, listen addresses, and admin role.
Outputs: a configured PostgreSQL service and managed `pg_hba.conf`.
