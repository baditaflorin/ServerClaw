all:
  children:
    build_server:
      hosts:
        build-lv3:
          # Replace this placeholder with the real build-server Tailscale IPv4.
          ansible_host: 100.64.0.1
          ansible_user: ops
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
